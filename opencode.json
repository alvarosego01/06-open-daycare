{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "playwright": {
      "type": "local",
      "command": [
        "npx",
        "-y",
        "@playwright/mcp"
      ],
      "enabled": true
    }
  },
  "references": {
    "db-schema": {
      "path": "../07-DB-Schema",
      "description": "Schema de base de datos, tablas, columnas y referencias a crear, esto no está implementado en la base de datos, es solo para referencia"
    }
  },
  "lsp": true,
  "permission": {
    "external_directory": {
      "/tmp/**": "allow"
    },
    "bash": "allow"
  },
  "command": {
    "spec": {
      "description": "Crea especificaciones de pantallas y funcionalidades",
      "template": "Carga y sigue la skill spec en .agents/skills/spec/SKILL.md.\n\nFeature: $ARGUMENTS"
    },
    "db-migrate": {
      "description": "Gestiona migraciones de base de datos: detecta gaps, genera SQL, aplica y verifica integridad",
      "template": "Carga y sigue el agente db-migrator en .opencode/agents/db-migrator.md.\n\nTarea: $ARGUMENTS"
    },
    "a11y": {
      "description": "Revisa y corrige accesibilidad WCAG 2.2 AA en un componente",
      "template": "Carga y sigue el agente accessibility-checker en .opencode/agents/accessibility-checker.md.\n\nArchivo: $ARGUMENTS"
    }
  }
}